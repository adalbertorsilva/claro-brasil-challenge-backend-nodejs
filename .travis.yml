language: node_js
services:
- postgresql
addons:
  postgresql: 9.6
  apt:
    packages:
    - postgresql-9.6-postgis-2.3
before_script:
- psql -c 'CREATE DATABASE claro_device_test;' -U postgres
env:
  - DEVICE_LIMIT=3
  - DEVICE_CHANGE_WAIT_PERIOD=30
